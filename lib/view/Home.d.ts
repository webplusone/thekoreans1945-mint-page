import { View, ViewParams } from "skydapp-common";
export default class Home implements View {
    private container;
    private interval;
    private preMintTime;
    private wlMintTime;
    private publicMintTime;
    private mintForm;
    constructor();
    private load;
    private displayMintForm;
    changeParams(params: ViewParams, uri: string): void;
    close(): void;
}
//# sourceMappingURL=Home.d.ts.map